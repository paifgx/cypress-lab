# Frontend dev server port
VITE_PORT=5173

# API base URL exposed to the client
VITE_API_URL=http://localhost:3001

# Toggle MSW-based API mocking in development (on by default)

# Optional global latency applied by the mock middleware (milliseconds)
VITE_API_DELAY_MS=0